@testable import AssetTransferFeature
import FeatureTestingPrelude

@MainActor
final class AssetTransferFeatureTests: TestCase {
	func test() {
		// TODO: I just added this test case so CI can keep the build healthy for AssetTransferFeature module
		// WHen AssetTransfer is properly implemented we should write tests here.
	}
}
